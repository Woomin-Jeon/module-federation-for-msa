{
  "private": true,
  "workspaces": [
    "container",
    "app1",
    "app2",
    "store"
  ],
  "scripts": {
    "build": "yarn turbo run build",
    "build:container": "yarn workspace @module-federation/container build",
    "build:app1": "yarn workspace @module-federation/app2 build",
    "build:app2": "yarn workspace @module-federation/app2 build"
  },
  "devDependencies": {
    "turbo": "^1.2.14"
  },
  "packageManager": "yarn@3.1.1"
}
